import Button from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "Purchase Now!"
  }
};

export const PurchaseLater = {
  args: {
    label: "Purchase Later!!!!"
  }
};